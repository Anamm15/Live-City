export type Reward = {
  id: string;
  name: string;
  description: string;
  point: number;
  image: string;
};

export type RewardResponse = {
  id: string;
  name: string;
  description: string;
  point: number;
  image: string;
};
